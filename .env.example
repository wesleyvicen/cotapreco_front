VITE_API_URL=http://localhost:8080/api
VITE_TRIAL_DAYS=15

# A chave do Asaas NÃO entra aqui. Tudo que é prefixado com VITE_ vai para o bundle e
# é público — qualquer visitante lê no navegador. A cobrança roda no backend, e as
# variáveis dela estão no .env.example do repositório cotapreco_back.
